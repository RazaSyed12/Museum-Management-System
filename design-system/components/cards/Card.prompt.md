Base surface: white, 1px subtle border, 10px radius, extra-small shadow; lifts 2px on hover when `interactive`.

```jsx
<Card interactive as="a" href="/collections/dinosaurs" padding={0}>…</Card>
```
