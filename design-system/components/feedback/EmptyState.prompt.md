Shown when a filter, search, booking history or staff table returns nothing.

```jsx
<EmptyState title="No collections match those filters"
  description="Try clearing a period or category."
  action={<Button variant="secondary">Clear filters</Button>} />
```
