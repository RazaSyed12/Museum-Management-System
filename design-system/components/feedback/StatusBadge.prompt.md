Pill status marker. Always icon + word, so status never depends on colour alone.

```jsx
<StatusBadge tone="warning">Limited availability</StatusBadge>
<StatusBadge tone="danger">Cancelled</StatusBadge>
<StatusBadge tone="member">Member price</StatusBadge>
```
