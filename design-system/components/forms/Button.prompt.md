The action primitive — every clickable command in the museum site and staff portal.

```jsx
<Button variant="primary" iconLeft="ticket">Book tickets</Button>
<Button variant="secondary">Explore exhibition</Button>
```

One `primary` per view. Use `accent` only on dark-green surfaces. `sm` is reserved for staff tables — everything visitor-facing keeps the 44px touch target.
