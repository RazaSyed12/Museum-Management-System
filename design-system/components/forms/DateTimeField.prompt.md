Date / time entry for event scheduling and visit planning. Uses the native picker so keyboard and screen-reader behaviour comes free.

```jsx
<Field label="Start date" htmlFor="start"><DateTimeField id="start" type="date" /></Field>
```
