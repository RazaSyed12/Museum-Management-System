Square icon-only action for headers, toolbars and card overlays.

```jsx
<IconButton icon="search" label="Search the collection" variant="ghost" />
```

`inverse` is for placement over photography or the dark-green header. `label` is mandatory.
