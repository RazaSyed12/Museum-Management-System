Dropzone for collection, item and event imagery in the staff portal.

```jsx
<Field label="Cover image" htmlFor="img"><ImageUpload fileName={file} onRemove={clear} /></Field>
```

States: empty, drag-over, filled, error.
