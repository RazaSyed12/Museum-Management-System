Single-line text field — always wrapped in `Field`.

```jsx
<Field label="Card number" htmlFor="card" error="Enter a 16-digit card number">
  <Input id="card" invalid inputMode="numeric" />
</Field>
```
