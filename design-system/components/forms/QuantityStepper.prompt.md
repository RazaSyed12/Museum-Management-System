Ticket quantity control on the ticket-selection screen.

```jsx
<QuantityStepper label="General admission" value={n} max={remaining} onChange={setN} />
```

Disable `+` at remaining availability rather than showing an error afterwards.
