Renders a Lucide glyph tinted with currentColor — the only icon primitive in this system.

```jsx
<Icon name="ticket" size={20} label="Tickets" />
```

Never hand-write SVG paths; pass a Lucide slug. Decorative icons beside a text label should omit `label`.
