The Heritage Museum brand mark in its three approved lockups.

```jsx
<Logo variant="horizontal" height={44} href="/" assetBase="assets/" />
```

Use `horizontal` in headers ≥768px, `mark` on mobile headers and favicons, `primary` on hero/print/marketing surfaces. Never recolour, stretch, or rebuild the mark.
