Image frame for artefact and exhibition photography, with a duotone placeholder when no shot exists yet.

```jsx
<Media ratio="16 / 9" tone="olive" caption="Bronze Age hoard, gallery 4" scrim />
```

Always set `scrim` when text sits on top of the image.
