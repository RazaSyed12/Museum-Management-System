Numbered pagination on desktop, "Load more" on mobile listings and staff tables.

```jsx
<Pagination page={2} pages={8} onChange={setPage} />
<Pagination showLoadMore resultLabel="Showing 24 of 96 collections" onLoadMore={more} />
```
