Dark-green site footer carrying visit info, opening hours, contact, social and legal links.

```jsx
<SiteFooter assetBase="assets/" />
```

Collapses to a single column below 768px (grid columns auto-stack when the container narrows).
