The visitor site header — same structure at every breakpoint, progressively reduced.

```jsx
<SiteHeader active="Collections" user={{name:'Amara O.', initials:'AO'}} isMember />
```

Desktop: full nav + Book tickets. Tablet: first four links. Mobile: hamburger + circular mark + search + account.
