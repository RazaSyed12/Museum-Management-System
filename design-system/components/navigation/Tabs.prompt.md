Section switcher — profile sections, event time-ranges, staff record tabs.

```jsx
<Tabs variant="pill" value={t} onChange={setT}
  items={[{value:'now',label:'On now',count:6},{value:'soon',label:'Upcoming',count:12}]} />
```
